What is the difference between a merge and a rebase?
Answer: While a merge is non-destructive, a rebase is destructive. Another difference is that merge preserves history as it happened, but rebase rewrites it.

What is a remote repository?
Answer: remote repository is a version of your project that are hosted on the Internet or network somewhere.

